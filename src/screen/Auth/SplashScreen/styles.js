import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '25%',
    height: 30,
    resizeMode: 'contain',
    alignSelf: 'center',
  },
});

export default styles;
